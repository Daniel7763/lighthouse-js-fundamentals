const smartGarbage = function (trash, bins) {
    
  if (trash === "compost"){
    bins.compost ++
  }
  if (trash === "recycling"){
    bins.recycling ++
  }
  if (trash === "waste"){
    bins.waste ++
  }
  return bins
} 

smartGarbage('recycling', { waste: 10, recycling: 10, compost: 10 });